[paths]
# Paths are relative to THIS config file (testdata/).
vertices = "vertices.txt"
edges = "edges.txt"
ranks = "ranks.txt"
results_dir = "results"

rank_metric = "pagerank"

[[targets]]
domain = "https://example.com/"

# Intentionally absent from vertices — demo of found=false stub
[[targets]]
domain = "https://not-in-graph.test/"
